version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: portal-rh-frontend
    ports:
      - "3001:80"
    networks:
      - portal-rh-network
    restart: always

networks:
  portal-rh-network:
    driver: bridge
